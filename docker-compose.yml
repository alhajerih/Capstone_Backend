version: '3.8'

networks:
  my_network:
    driver: bridge

services:
  postgres:
    image: postgres
    container_name: postgres_container
    restart: always
    ports:
      - "5435:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - my_network

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin4_container
    restart: always
    ports:
      - "8888:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - my_network

  redis:
    image: redis:latest
    container_name: redis_container
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - my_network

  eureka-server:
    build: ./eureka-server
    container_name: eureka_server
    ports:
      - "8761:8761"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    networks:
      - my_network

  api-gateway:
    build: ./api-gateway
    container_name: api_gateway
    ports:
      - "8765:8765"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - SPRING_REDIS_HOST=redis
      - SPRING_REDIS_PORT=6379
    depends_on:
      - eureka-server
      - redis
    networks:
      - my_network

  shares-app:
    build: ./Shares
    container_name: shares_container
    ports:
      - "8080:8080"
    depends_on:
      - postgres
      - eureka-server
      - redis
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/postgres
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - SPRING_REDIS_HOST=redis
      - SPRING_REDIS_PORT=6379
    networks:
      - my_network

  notificationservice-app:
    build: ./notificationservice
    container_name: notificationservice_container
    ports:
      - "8081:8081"
    depends_on:
      - postgres
      - eureka-server
      - redis
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/postgres
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=postgres
      - SPRING_REDIS_HOST=redis
      - SPRING_REDIS_PORT=6379
    networks:
      - my_network

#  expo-app:
#    build: ./Capstone_MobileFrontend  # Path to Expo project
#    container_name: expo_app_container
#    ports:
#      - "19000:19000"  # Metro bundler
#      - "19001:19001"  # Expo DevTools
#      - "19002:19002"  # Expo WebSocket connection
#    volumes:
#      - ./Capstone_MobileFrontend:/app  # Mount project files for live changes
#      - /app/node_modules
#    stdin_open: true
#    tty: true
#    depends_on:
#      - api-gateway
#    networks:
#      - my_network

volumes:
  postgres_data:
  pgadmin_data:
  redis_data:
